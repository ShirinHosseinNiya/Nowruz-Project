package org.project.account.user;

public class SimpleUser extends User {
    public SimpleUser(String firstName, String lastName, int age, String email, String username, String password) {
        super(firstName, lastName, age, email, username, password);
    }
}